import SectionTitle from "../../../Components/SectionTitle/SectionTitle";

const Banner = () => {
    return (
        <div>
            <SectionTitle title={"check"} subTitle={"check"}></SectionTitle>
        </div>
    );
};

export default Banner;